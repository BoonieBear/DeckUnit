<controls:ContentPage x:Class="BoonieBear.DeckUnit.Views.AcousticView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:metroControl="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
      xmlns:controls="clr-namespace:BoonieBear.DeckUnit.Controls"
      DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=AcousticViewModel}"
      KeepAlive="True"
      mc:Ignorable="d" 
	 d:DesignHeight="480" d:DesignWidth="640"
	 Title="水声通信"  >

    <Grid Background="{StaticResource MainBackGround}">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Command="{Binding GoHomeCMD}" Height="20"  Width="20" VerticalAlignment="Center" Margin="20">
                <Button.Background>
                    <ImageBrush ImageSource="/Assets/left.png" Stretch="Uniform" />
                </Button.Background>
                </Button>
                <TextBlock  Text="声学指令" VerticalAlignment="Center" FontFamily="Segoe UI" FontSize="34px"/>
        </StackPanel>
        
    </Grid>
</controls:ContentPage>
